---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/App.css
  - src/index.css
  - src/lib/supabase.ts
  - src/types/index.ts
  - src/components/AuthForm.tsx
  - src/pages/Login.tsx
  - src/pages/Dashboard.tsx
  - .env.local
  - .gitignore
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "User can visit /login and see an email/password sign-in form labeled UW Login"
    - "User with @wisc.edu email can sign up and sign in"
    - "After sign-in, user is redirected to /dashboard"
    - "Unauthenticated user visiting /dashboard is redirected to /login"
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "Supabase client singleton"
      exports: ["supabase"]
    - path: "src/types/index.ts"
      provides: "Shared TypeScript types for Canvas and chat exports"
      exports: ["CanvasCourse", "ChatExport"]
    - path: "src/components/AuthForm.tsx"
      provides: "Email/password auth form component"
      min_lines: 30
    - path: "src/pages/Login.tsx"
      provides: "Login page with AuthForm"
    - path: "src/pages/Dashboard.tsx"
      provides: "Auth-gated dashboard shell"
    - path: "src/App.tsx"
      provides: "Router with /login and /dashboard routes"
  key_links:
    - from: "src/components/AuthForm.tsx"
      to: "src/lib/supabase.ts"
      via: "supabase.auth.signInWithPassword / signUp"
      pattern: "supabase\\.auth\\.(signIn|signUp)"
    - from: "src/App.tsx"
      to: "src/pages/Login.tsx"
      via: "react-router-dom Route"
      pattern: "Route.*path.*login"
    - from: "src/pages/Dashboard.tsx"
      to: "src/lib/supabase.ts"
      via: "supabase.auth.getUser for auth guard"
      pattern: "supabase\\.auth\\.getUser"
---

<objective>
Scaffold the ConnectUW project from scratch and implement the auth flow so users can sign in with a @wisc.edu email and land on an authenticated dashboard.

Purpose: Establishes the entire project foundation -- build tooling, routing, Supabase connection, and the auth gate that every subsequent feature depends on.
Output: A running Vite + React app with login/dashboard routes, Supabase auth, and shared type definitions.
</objective>

<execution_context>
@/Users/tylersue/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tylersue/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/research/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React project with all Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/App.css, src/index.css, src/lib/supabase.ts, src/types/index.ts, .env.local, .gitignore</files>
  <action>
1. Run `npm create vite@latest . -- --template react-ts` in the project root (the project dir already exists, scaffold into it).
2. Install all Phase 1 dependencies:
   ```
   npm install @supabase/supabase-js react-router-dom zod lucide-react zustand
   npm install tailwindcss @tailwindcss/vite
   npm install -D vitest
   ```
3. Configure Tailwind v4 in vite.config.ts -- add `@tailwindcss/vite` plugin:
   ```typescript
   import { defineConfig } from 'vite'
   import react from '@vitejs/plugin-react'
   import tailwindcss from '@tailwindcss/vite'

   export default defineConfig({
     plugins: [react(), tailwindcss()],
   })
   ```
4. Update src/index.css to import Tailwind v4:
   ```css
   @import "tailwindcss";
   ```
5. Create src/lib/supabase.ts -- Supabase client singleton:
   ```typescript
   import { createClient } from '@supabase/supabase-js'
   export const supabase = createClient(
     import.meta.env.VITE_SUPABASE_URL,
     import.meta.env.VITE_SUPABASE_ANON_KEY
   )
   ```
6. Create src/types/index.ts with shared types:
   ```typescript
   export interface CanvasCourse {
     id: number
     name: string
     course_code: string
     enrollment_term_id: number
   }

   export interface ChatExport {
     id: string
     user_id: string
     raw_json: unknown
     source: 'claude' | 'chatgpt'
     created_at: string
   }

   export interface UserSettings {
     user_id: string
     canvas_token: string | null
     courses: CanvasCourse[] | null
     updated_at: string
   }
   ```
7. Create .env.local with placeholder Supabase vars:
   ```
   VITE_SUPABASE_URL=https://your-project.supabase.co
   VITE_SUPABASE_ANON_KEY=your-anon-key
   ```
8. Add .env.local to .gitignore (append if .gitignore exists from Vite scaffold).
9. Run `npx shadcn@latest init` -- accept defaults (this sets up shadcn/ui config).
10. Run `npx shadcn@latest add input button card` to install the three UI components needed for Phase 1.
11. Verify: `npm run dev` starts without errors.
  </action>
  <verify>
    <automated>cd /Users/tylersue/Downloads/code/ClaudeHacks2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Vite dev server starts, TypeScript compiles, Tailwind processes styles, Supabase client exports, types file exports CanvasCourse/ChatExport/UserSettings, shadcn/ui components available.</done>
</task>

<task type="auto">
  <name>Task 2: Implement auth flow -- AuthForm, Login page, Dashboard shell, route guard</name>
  <files>src/components/AuthForm.tsx, src/pages/Login.tsx, src/pages/Dashboard.tsx, src/App.tsx, src/main.tsx</files>
  <action>
1. Create src/components/AuthForm.tsx:
   - Email input (shadcn Input) + password input + submit Button
   - Two modes: "sign-in" and "sign-up" with toggle link
   - On submit: call `supabase.auth.signInWithPassword({ email, password })` for sign-in, `supabase.auth.signUp({ email, password })` for sign-up
   - Client-side check: if email does not end with `@wisc.edu`, show inline error "Only @wisc.edu email addresses can sign in" and do NOT call Supabase (this is the primary gate; the Supabase DB hook is a server-side backup the user can configure later)
   - Show error messages from Supabase (e.g., "Invalid login credentials") in a red text div below the form
   - Show loading spinner (lucide-react Loader2 with animate-spin) on submit button while request is in flight
   - On successful auth, navigate to /dashboard using react-router-dom useNavigate

2. Create src/pages/Login.tsx:
   - Centered layout with Card component wrapping AuthForm
   - Title: "ConnectUW" in large text, subtitle: "Sign in with your UW NetID"
   - Render AuthForm inside the Card

3. Create src/pages/Dashboard.tsx:
   - Auth guard: on mount, call `supabase.auth.getUser()`. If no user, `navigate('/login')`.
   - Listen for auth state changes with `supabase.auth.onAuthStateChange` -- redirect to /login on SIGNED_OUT event
   - Show greeting: "Welcome, {user.email}" 
   - Add a "Sign Out" button that calls `supabase.auth.signOut()` then navigates to /login
   - Leave placeholder sections for Canvas integration and Chat upload (these will be added in Plan 02):
     - A section titled "Canvas Courses" with text "Connect your Canvas account to see your courses"
     - A section titled "Chat History" with text "Upload your Claude or ChatGPT chat export"
   - Use a clean layout: header with title + sign out, then two Card sections stacked vertically

4. Update src/App.tsx:
   - Set up BrowserRouter with Routes:
     - `/login` -> Login page
     - `/dashboard` -> Dashboard page
     - `/` -> redirect to /dashboard (Navigate component)
   - Remove all Vite boilerplate content

5. Update src/main.tsx to wrap App in BrowserRouter (or use createBrowserRouter -- either pattern is fine, but be consistent with how App.tsx sets up routes).

6. Clean up: remove src/App.css content (Tailwind handles styling), remove any Vite logo imports.
  </action>
  <verify>
    <automated>cd /Users/tylersue/Downloads/code/ClaudeHacks2 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Login page renders at /login with email/password form. Non-@wisc.edu emails rejected client-side with error message. Successful auth redirects to /dashboard. Dashboard shows user email and sign-out button. Visiting /dashboard without auth redirects to /login. Two placeholder sections visible for Canvas and Chat upload.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without TypeScript or build errors
2. `npm run dev` starts and serves the app
3. Navigate to localhost:5173 -- redirects to /login
4. Login form visible with email/password fields
5. Entering a non-@wisc.edu email shows client-side error
</verification>

<success_criteria>
- Project scaffolded with all Phase 1 dependencies installed
- Supabase client configured and exported
- Shared types (CanvasCourse, ChatExport, UserSettings) exported from src/types/index.ts
- Auth form with @wisc.edu validation renders at /login
- Dashboard with auth guard renders at /dashboard
- Route protection works (redirect unauthenticated users)
- App builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
